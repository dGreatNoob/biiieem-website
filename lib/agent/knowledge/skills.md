# Skills and stack

## Languages

TypeScript, JavaScript, PHP, SQL, Python (growing), Bash. Some C++ from his Computer Engineering background (embedded / microcontrollers).

## Frontend

- Next.js 16 (App Router, React 19)
- Tailwind CSS v4, DaisyUI
- Livewire / Flux (PHP full-stack reactive UI)
- Blade templates

## Backend

- Node.js, Next.js API Routes
- Laravel 12 (primary PHP framework)
- REST APIs, webhooks, Server-Sent Events (SSE) for streaming

## Databases and data infrastructure

- **Supabase** — Postgres, Auth, Storage, Realtime
- PostgreSQL, MySQL 8
- Row-Level Security (RLS) for multi-tenant isolation
- pgvector for embeddings / RAG retrieval
- Redis (caching + queues)
- Laravel Reverb (WebSockets)
- Partial unique indexes for concurrency safety (e.g., preventing double-booking)
- HMAC signing, rate limiting

## AI / LLM

- **LangGraph** — state machines, deterministic node flows, conditional routing
- **OpenRouter** — provider-agnostic LLM routing (Gemini, Claude, GPT)
- **RAG pipelines** — chunking, embeddings (`baai/bge-m3`), pgvector cosine-similarity retrieval, operator-visible citations
- Prompt engineering, streaming inference, tool-use, correction-pair training loops
- **MCP servers** — integration experience
- Evaluation: prompt regression sandboxes, correction-pair training UIs

## Integrations

Resend (transactional email), Semaphore (PH outbound SMS), Stripe, generic third-party APIs / webhooks.

## DevOps / Tools

Vercel (primary deploy target), Git, GitHub Actions, Docker, Claude Code, Cursor.

## Design / UI

Tailwind CSS v4 opinionated, DaisyUI for component scaffolding. Comfortable implementing designs from Figma; not a designer-designer.

## What he is NOT strong in (yet)

- Deep ML training / PyTorch (can integrate pretrained models via API; hasn't trained foundation models)
- Native mobile (iOS/Android SDK)
- Kubernetes orchestration at scale (has used Docker Compose and Vercel; not a k8s specialist)

If the visitor asks about a specific technology not listed here, route to email rather than guessing.
